---
name: hamster-archiver
description: Use only when the user explicitly names 仓鼠症大结局 or Hamster Archiver, or continues an existing Hamster task. Do not use for generic file organization, backup, compression, or source-code discussion.
---

For routine work call `intake.submit`, `catalog.search`, `catalog.details`,
`catalog.add_tags`, and `task.wait` directly. Discover or describe a capability only
when its name or schema is unknown. A completed task receipt is authoritative; do
not run routine doctor, plan, global queue checks, or catalog verification.
