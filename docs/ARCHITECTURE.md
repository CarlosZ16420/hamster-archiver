# 架构说明

## 运行边界

Electron 主进程位于 `src/main.js`，负责窗口、IPC、文件系统、队列和更新流程；预加载层只暴露受控 API；`src/renderer/` 管理界面、状态和 i18n，其中无 Electron 依赖的 i18n 模块也供主进程原生弹窗复用；`src/core/` 保存可独立验证的业务与数据模块。

```text
Renderer / i18n
       ↓ IPC
Main process
       ├─ queue + archive engine ── 7-Zip / FFmpeg
       ├─ SQLite repository ─────── warehouse + thumbnails
       ├─ application task service ─ CLI / MCP / UI receipts
       ├─ integration manager ───── Codex / WorkBuddy / MCP / ODR preview
       ├─ storage migration ─────── userdata layout
       └─ updater ───────────────── GitHub → CNB fallback / local package + digest + rollback
```

## 源码目录

- `src/core/`：归档、路径、SQLite、相似度、媒体、回收站和更新。
- `src/renderer/`：页面行为、UI 状态与本地化。
- `src/renderer/catalog-columns.js`：列表表头列宽比例调整与本机界面偏好，独立于 IPC 和仓库资料；兼容读取旧像素列宽，将当前可用宽度按比例分配，通过共享 CSS 变量同步表头与正文，并保持末列固定和表格总宽不变。
- `scripts/`：依赖恢复、检查、构建、版本维护和公开快照。
- `test/`：使用虚构数据和临时目录的自动测试。
- `tools/`：锁定工具的许可证、清单及允许提交的小型组件。
- `docs/`：产品、架构、UI、发布和历史资料。

## 数据边界

更新检查通过来源适配器统一生成版本、说明、历史、附件、摘要、`provider` 与发布页。版本化只读配置位于 `src/config/update-providers.json` 并随包进入完整性清单；内置 CNB 目标使用公开 Release 页的 latest 跳转发现版本，并从公开 latest/download 直链取得附件和 SHA-256，不向客户端提供 Token。主进程先查权威 GitHub，只有网络、超时、HTTP 或解析失败才查 CNB；GitHub 成功（含已最新）即短路。GitHub 手动检查从同一来源分页获取历史（100 条/页，最多 10 页，共用超时）；CNB 公开回退不调用需鉴权的 OpenAPI，保留 latest 并把历史标为不完整。渲染层 `update-dialog.js` 负责安全呈现双语 Markdown 子集，主进程缓存包含来源标记的下载元数据；安装 IPC 仅接收目标版本并校验缓存，不接受渲染层提供地址。下载管理器按 `provider` 使用相互独立的主机白名单，所有重定向均使用 manual 模式并在请求下一跳前校验 `Location` 主机，仍执行版本、manifest、SHA-256 和回滚约束。静默检查不弹窗、不加载历史，也不覆盖用户已查看的安装目标。

打包应用通过同目录 `user-data-location.json` 解析用户数据；没有指针时保持普通便携版的同目录 `userdata/` 行为。本项目维护机上的 `builds/current` 使用相对指针连接仓库外 `data/production`。开发模式固定使用仓库外 `data/development`，不会再读写源码根目录。

打包应用启动时尽早显示独立的轻量启动窗口，再检查用户数据区中的发行完整性缓存。缓存同时绑定发行清单摘要、构建提交、程序位置以及每个受检文件的大小、修改时间、变更时间和文件身份；任一条件变化即在工作线程重新执行完整 SHA-256 校验，成功后原子刷新缓存。缓存只优化启动，下载、更新与发行验收仍无条件完整校验。普通桌面启动在设置、队列安全状态和 IPC 就绪后显示主窗口，完整仓库记录由只读工作线程按有界批次解析，界面明确显示加载进度；依赖仓库的搜索、详情与写操作在加载成功前等待同一个就绪任务，避免暴露部分目录。MCP 后台、烟雾和完整性验收仍等待完整仓库初始化后再声明就绪。

持久数据变更必须经过 `storage-paths` 与 `storage-migration`，保持旧布局可识别、迁移可回退。`warehouse-paths` 专门负责缩略图引用的相对化、旧绝对路径升级和当前仓库解析，禁止业务层直接把仓库内绝对路径写入 SQLite。任何 SQLite 结构变化都要考虑 WAL、事务和旧数据兼容。

运行日志以 JSON Lines 持久化在当前用户数据区的 `logs/app.log`。写入按调用顺序串行，正常退出前等待在途日志落盘；启动时读取最近 300 条有效记录，单条损坏或进程中断留下的截断行不会遮蔽更早记录。日志覆盖任务阶段之外的启动与正常退出、设置字段变化、用户数据区切换、更新操作、相似度排除词维护、仓库删除失败和关键后台错误；设置日志只记录发生变化的字段名称，不记录密码值。只读浏览、搜索、复制和打开位置不写日志，避免把普通查看行为变成噪声或额外隐私记录。

## 构建边界

队列缩略图由 `thumbnail-service` 调度，图片最多两个并发，FFmpeg 视频保持单进程顺序抽帧。输出名与数量槽位在派发前预留，取消后排空在途图片任务再补偿，避免清理与写入竞争。尝试预算为 `max(30, thumbnailLimit × 3)`；单帧最多 30 秒，单视频抽帧累计活动时间最多 120 秒（探测另限 30 秒，暂停不计入进程时限），失败保留成功帧并记录明确警告。入库只保存新记录及双向相似关系受影响的记录，回滚快照只保留受影响关系字段和原目录引用，不复制历史清单；SQLite 对比持久 JSON 确认仅关系字段变化时保留文件、搜索和指纹索引，无结构迁移。阶段日志区分预览、相似关系与数据库耗时，队列显示处理数量及当前视频帧。

仓库详情的精确文件查询在排序限额之前排除项目自身，只传递文件级字段，每个匹配项目的 JSON 元数据每次查询只解析一次；不修改 SQLite 结构。渲染层用 IntersectionObserver 按可见区域调度缩略图，同时最多读取 4 张，复用现有缓存；目录继续使用虚拟树。

可选 MCP 入口由 `src/core/mcp-server.js` 在显式启用后监听本机随机端口；发行包根目录的薄 `.cmd` 使用 Electron 内置 Node 运行 `mcp-client.js`。正式 `hamster.cmd` CLI 与 MCP 共同调用 `application-task-service`，由它冻结任务级参数、先处理 requestId 幂等、保存 jobIds 并生成统一回执；两者不直接写 SQLite 或复刻归档流程。`automation-definitions.js` 是稳定入口、状态和构建清单的中央定义，发行包的 `ai-capabilities.json` 由构建生成。Windows 桌面代理与直接回退都会清除派生进程的 Node 模式环境并关闭崩溃上报，但保留 Electron 的系统默认图形选择与 Chromium 渲染沙箱。已有普通实例通过 `second-instance` 启用 MCP，始终只有一个仓库写入者。

stdio 客户端用内部租约心跳管理后台实例生命周期：就绪后首次连接宽限 30 秒，最后会话断开宽限 60 秒，活动 AI 任务继续保活。后台实例提供托盘入口；用户打开共享工作台后转为桌面生命周期。连接文件使用随机 token 与 instanceId 绑定 PID、启动时间和版本，退出时只清理本实例拥有的连接。loopback HTTP 只作为启动器内部认证桥接，不作为稳定远程 MCP 地址。

`mcp-tools.js` 只公开发现、描述和调用三个紧凑工具，稳定高层 capability 为 `intake.submit` 与 `task.get/wait/resolve/retry/cancel`；旧批次和队列回执作为兼容包装保留。应用任务接受显式路径时绕过自动扫描体积过滤，所有启动、确认、重试与取消只作用于本任务 jobIds；异步错误也写回任务。请求账本位于用户数据区，与 SQLite 仓库格式分离，不要求数据库迁移。`integration-manager.js` 只在用户启用后写入带摘要/标记的自有 Skill 或宿主配置，升级、修复和卸载遇到用户修改即停止；ODR 在缺少安全 package identity 时保持预览且不要求降低 Windows 保护。详见 [CLI](CLI.md) 与 [MCP](MCP.md)。

`scripts/build-release.js` 只写入仓库外 staging。`scripts/release-local.js` 从干净提交按需生成 Current、ZIP 或安装版，并把阶段凭据写入仓库外 `builds/release-runs`；所有产物只做一次最小隔离启动，旧 Current 进入 history。正式 Release 入口先识别远端状态，云端预检、构建、上传分作业续跑。公开 GitHub 和 CNB 从上游 Release 镜像同一附件，不参与构建。源码根目录不保存 Electron 运行时副本。

每个发行包从 `docs/releases/release-summary-vX.Y.Z.json` 把中英文更新内容写入 `release-manifest.json`。在线检查读取成功来源的 Release 正文，手动 ZIP 读取包内清单；安装版在线升级选择同版本 Setup EXE 并校验同来源 SHA-256，手动升级只接受严格命名且高于当前版本的 Setup EXE。CNB 同步是 GitHub 正式发布后的独立两阶段流程，并采用默认拒绝写入边界：目标、当前官方 API 契约、上传主机和 Token 实际权限未独立验证前，`CNB_SYNC_ENABLED` 保持关闭，脚本不把权限声明字符串视为授权证明。启用后只能创建/接续草稿，复制权威正文和四个已校验附件，完整回读后再发布；创建草稿永不提升 latest，历史补同步默认也不提升，只有调用方确认目标为当前最新正式版本时才显式选择。workflow 固定运行 `github.sha` 中的同步代码，标签仅用于 API 选择；同步不参与构建，也不推送源码。上传或发布响应不明时先按名称、大小和 SHA-256 回读，确认缺失后才重试。两种发行形态都会在启动更新助手或安装程序前，把受限长度的说明写入本次用户数据区 `updates/` 运行目录。新版本只接受该受信任目录且目标版本与自身一致的提示文件，在首次启动时显示后移除本次临时目录，不新增长期用户状态。

队列相似报告通过只读 IPC 按任务标识读取待确认清单；尚未生成本次任务的 MD5 时只枚举当前目录，不因打开弹窗而读取大文件内容，也不凭普通历史路径、大小或修改时间复用 MD5 来报告内容完全一致。拖入或扫描时的名称、标题和视频大小候选只显示非阻塞提示，不产生“已确认”状态；用户选择入库方式并启动队列后，应用先生成完整结构快照，再进行相似与重复核验。同一规范化原始位置的仓库记录若具有完整可信的旧快照，且当前与旧快照的全部文件相对路径、大小、数量、总大小以及目录清单（含空目录）一致，可在读取文件内容前判为项目完全重复；任一侧有跳过项、计数或大小不一致、缺少目录清单时不得走该快捷路径。项目完全重复且开启自动跳过时直接跳过；不完全重复但仍有相似证据时只进行一次清单后确认，确认结果以路径、大小、MD5 和修改时间组成的清单指纹绑定，并复用已保存清单继续处理。旧版本任务仍兼容原预检确认字段。

SQLite 为每条仓库记录维护项目级形状指纹和完整内容指纹：完整清单优先通过内容索引直接定位，部分清单先通过形状索引缩小候选，再选择区分度高且较小的文件补算 MD5，所有候选排除后立即停止。同原始位置的结构快捷路径在形状索引候选上继续核对可信快照与完整目录清单；不满足条件的同路径旧记录不会再把当前路径当成历史内容来源。只有旧记录自身缺少 MD5 且仍有不同的可访问来源时，才在一个当前项目等量的共享读取预算内核验；预算不足转人工复核且绝不自动跳过。文件内容完全一致使用分批索引查询，报告和仓库详情复用查询结果，不再逐文件扫描整库。报告与仓库详情共同使用 `findSimilarEntryMatches` 和渲染层虚拟目录树，避免两套标记规则发生漂移。

队列的“卡顿规避”只缩减普通相似度持久保存的 MD5 数量，不缩减归档清单，也不把未抽样文件排除出名称、路径、大小等诊断：超大文件夹仍完整压缩并保存全部路径、大小与修改时间。代表文件数量由任务快照中的 `largeFolderMd5SampleLimit` 决定，默认 200，采用稳定的“较大文件优先 + 全目录均匀覆盖”混合抽样；启用极小文件跳过时先按任务保存的 KB 阈值排除文件，再按用户设置补足最多 N 个样本，默认阈值为 5 KB。除上段限定的同原始位置完整结构快捷路径外，自动跳过使用独立精确核验：当前任务与候选都必须补齐有效 MD5，并在受控读取预算内核验；只有所有路径、大小与 MD5 都得到完整证明才会自动跳过，禁止以同名、同大小、抽样结果或预算不足冒充完整一致。

队列每次启动都会固定本轮任务标识集合；运行期间新增、扫描完成或从仓库追加的项目标记为“等待下次入库”，不会被当前循环拾取。下一次显式或计划启动重新拍摄批次快照并清除该标记。文件清单与目录清单由同一次源树遍历产生，避免大型目录在 MD5 前后重复枚举。归档发布会用设备标识与盘符共同预判存储卷：同卷使用原子重命名，跨卷仍必须复制全部字节并核对空间、复制前后源身份与目标大小；若重命名实际返回跨卷错误，复制前会补做目标空间检查，并以实际复制模式记录日志和结果。记录会保存发布模式与毫秒耗时，运行日志同时写明文件数。发布后的缩略图、相似关系、仓库写入和源文件后处理使用独立阶段文案，源文件状态回写只更新受影响的仓库记录。

从仓库把未压缩记录加入压缩队列时，渲染层只负责收集用户对备份位置冲突的选择；核心层根据旧记录、当前设置和该选择解析最终值，并把结果快照写入任务。批量逐项确认将冲突位置快照保存为 `backupLocationConfirmation` 并复用 `awaiting_confirmation`；`confirmJob` 接收明确的位置选择，只解除此项确认，不解除大任务风险确认，同批其他任务照常执行。任务完成时使用该快照回写同一仓库记录，避免运行期间的设置变化改变已排队任务。

未压缩目录更新与后续压缩共用 `sourceSnapshot`：快照记录相对路径、大小、修改时间、空目录、完整性及读取错误。任务排队阶段只检查根路径；真正执行时由 `scanSourceSnapshot` 完成唯一一次全量枚举，再由 `compareSourceSnapshots` 进行 O(n) 对比。仅新增的更新可直接继续；修改、删除、旧快照不完整或压缩前任意变化会把任务转为 `awaiting_source_change_confirmation`。完整差异保存在 SQLite 的 pending manifest 中，jobs 只保存摘要和快照标识；渲染层通过只读 IPC 获取报告，不重新访问文件系统。

确认覆盖后，清单生成直接复用本次扫描快照；大小和修改时间均未变化的文件沿用原记录的 MD5、缩略图和媒体信息，只有新增或变化文件重新读取。确认新建独立项目时排除同路径旧记录参与重复阻塞，但其余正常查重仍保留。提交继续采用先生成新结果、后原子替换同一仓库记录的路径；失败、取消和跳过均保持原记录及原文件不变。目录更新批次携带固定任务集合和一次性跳过定时窗口标记，避免启动无关队列任务。

仓库删除把本应用管理的压缩包和缩略图送入 Windows 回收站，并在仅限当前进程的撤回栈保存完整记录及恢复路径；不建立应用内回收站或跨重启恢复副本。删除和撤回通过串行短操作门控，窗口关闭与应用退出会等待在途操作完成。删除条目后同步清除队列中指向失效仓库记录的候选；已跳过、已取消、失败和已完成的历史任务不再作为新任务的重复候选。

Windows 回收站查询与恢复辅助进程统一通过 UTF-8 输出路径，避免中文位置被系统控制台编码损坏。批量撤回按每批最多 100 条查询完整路径集合，不截断后续条目；仍先检查回收站内容和原位置冲突，禁止覆盖同名文件。
