# Hamster Archiver 4.5.8

4.5.8 补齐了 4.5.7 新增主目录层级后的相似度交互。

## 主要变化

- 文件夹项目的原始主目录名称会与已关联相似项目的主目录及内部目录名称一起比较。
- 主目录名称中的相似片段会按原有规则标红，点击后可一键加入相似度白名单。
- 单字母、单汉字和其他低信息名称仍受现有相似算法门槛约束，不会因为显示主目录层级而重新产生百分百相似误报。

## 数据与更新说明

本版本不改变仓库数据结构和已有相似关系。主目录标红来自详情页的即时解释结果；加入白名单后不会自动触发全库重算。
